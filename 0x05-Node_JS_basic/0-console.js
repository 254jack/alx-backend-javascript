/**
 * A method which dispaly a message to the STDOUT
 * @param {String} message message displayed
 * @author Jackson Njoroge <https://github.com/254jack>
 */
const displayMessage = (message) => {
  console.log(message);
};

module.exports = displayMessage;
